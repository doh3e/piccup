<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.piccup.model.dao.resume.ResumeDao">
	
	<!-- 로그인 유저의 전체 이력서 조회 -->
 	<select id="selectResume" resultType="Resume">
 		SELECT *
 		FROM resume
 		WHERE user_id = #{userId}; 
	</select>

	<!-- 상세 게시글 조회 -->
 	<!-- <select id="selectOne" resultMap="boardMap" parameterType="int"> column명과 dto가 달라서 boardmap으로 지정함
		SELECT * FROM board
		WHERE id = #{id};	
	</select> -->
	
	<!-- application.properties설정으로 map 쓰지 않아도 reg_date, view_cnt가 잘 출력됨
	 mybatis.configuration.map-underscore-to-camel-case=true -->
 	<!-- <select id="selectOne" resultType="Board" parameterType="int"> column명과 dto가 달라서 boardmap으로 지정함
		SELECT * FROM board
		WHERE id = #{id};	
	</select>
 -->
	<!-- 게시글 등록 -->
  	<!-- <insert id="insertBoard" parameterType="Board">
		INSERT INTO board (title, writer, content)
		VALUES (#{title}, #{writer}, #{content});
	</insert>
 -->
	<!-- 게시글 삭제 -->
	<!-- <delete id="deleteBoard" parameterType="int">
		DELETE FROM board
		WHERE id = #{id};
	</delete>
	 -->
	 
	 <!-- 조회수 증가 -->
 	<!-- <update id="updateViewCnt" parameterType="int">
		UPDATE board
		SET view_cnt = view_cnt+1
		WHERE id = #{id}
	</update>
	 -->
	<!-- 업데이트 -->
 	<!-- <update id="updateBoard" parameterType="Board">
		UPDATE board
		SET title = #{title}, content = #{content}, reg_date = now()
		WHERE id = #{id};	
	</update>
 -->	
</mapper>
